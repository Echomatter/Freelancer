# Shared application contracts

Pure normalization and preference functions used by the web frontend and its
local backend. These modules do not own credentials, filesystem state, provider
clients or an alternative model router. There is only one supported application.
